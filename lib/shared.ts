export const appName = 'Nomu Docs';
export const siteUrl = 'https://docs.nomu.land';

/** Nomu web app (install PWA, trips, map). Marketing site may live on nomu.land. */
export const appUrl = 'https://app.nomu.land';
/** Public URL prefix for doc pages (metadata, `page.url`, markdown negotiation). */
export const docsRoute = '/';

/** Internal App Router segment where Fumadocs pages live (`app/docs/...`). */
export const internalDocsRoute = '/docs';

export const docsImageRoute = '/og/docs';
export const docsContentRoute = '/llms.mdx/docs';

